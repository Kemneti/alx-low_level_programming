#include "main.h"
#include <stdio.h>
/**
 * int _strlen - function that returns the length of a string
 * @s: The string 
 * Return: Nothing
 */
int _strlen(char *s)
{
	char s[];
	int i, length = 0;
	
	for (i = 0; string[i] != '\0'; i++)
        {
        	length++;
        }
result(length)
