#include "main.h"
#include <stdio.h>
/**
 * int _strlen - function that returns the length of a string
 * @s: The string 
 * Return: Nothing
 */
int _strlen(char *s)
{
	char str[100],i;
	
	for (i = 0; str[i]!='\0'; ++i);
        
result(i);
